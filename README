软件应该包含的功能
1，离线日志浏览，在线日志浏览
2，文件格式 zip txt log others
3，离线方式可以把多个日志解析到数据库然后再分析
4，支持模糊搜索
5，支持实时滚动
6，log分级显示 verbose debug info warn error fatal assert，不同颜色以及过滤显示
7，tag 列表，进程，log条数，过滤显示
8，尽最大可能显示log窗口
9，过滤窗口可以折叠，当前过滤信息显示在状态栏
10，log 分类，logcat（system main event radio）kernel others
11，自定义快速动作，
12，log缓存，并关联bug 信息 以及 简要说明 可以随时查看
13，同一个project可以包含多个log信息
14，可以自己修改配置文件
15，log预过滤规则设置
16，统计模块 按进程（线程），按TAG（单选，多选，非），按等级 按时间
17，跳转到行
18，跳转的导航条，按时间分，不同tag显示不同颜色，崩溃信息显示不同的高度
19，崩溃信息分析 ，java crash ，native crash ，anr，
java 堆栈，快速跳转
native 堆栈，符号源码对应分析，机器码反编译
20，activity 跳转分析，系统关键操作分析 ，时序图
21，GC 分析
22，log对比（待定）
23，使用json定义 过滤器，描述，捕获名称，类型，
24，过滤器可以时候用哪个json 序列化 ，清除所有过滤器，保存上一次过滤器，常用的过滤器以及名字
25，多视图显示，并排，以及Tab显示
26，书签功能
27，字体设置



